<%#
  Full calendar surface. Uses the whole content area instead of sharing it with
  the conversation list like the start page does.
%>
<div class="flex min-h-0 w-full">
  <%= render "pl_connect_workspace/calendar", dom_id: "pl_connect_full_calendar" %>
</div>
